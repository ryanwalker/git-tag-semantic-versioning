#!/usr/bin/env bash

# Commit and tag qa
git checkou qa

NEW_VERSION=`./bin/bump-patch-version`

echo "$NEW_VERSION" >> .tags

git add .tags
git commit -m "$NEW_VERSION"
git tag -am $NEW_VERSION "${NEW_VERSION}"
git push
git push --tags

git checkout master
git pull
git merge qa
